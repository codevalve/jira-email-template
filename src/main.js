import './style.css';
import { initializeUI } from './uiController.js';

// Initialize the UI and set up event listeners once the DOM is fully loaded
document.addEventListener('DOMContentLoaded', () => {
  initializeUI();
});
